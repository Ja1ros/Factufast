<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/4.3.1/cosmo/bootstrap.min.css">
  <link rel="stylesheet" href="css/main.css">
  <title>Facturas</title>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container p-2">
      <a class="navbar-brand" href="/"><h1>Gestión de Facturas</h1></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/list">Listar todas las facturas</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/new">Nueva</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container p-4">
    <div class="row">
      <% if (facturas.length > 0) { %>
        <% facturas.forEach(function(factura) { %>
          <div class="col-md-4">
            <div class="card">
              <div class="card-header bg-primary text-white text-center">
                <%= factura.title %>
              </div>
              <a href="<%= factura.image_url %>" target="_blank" style="text-decoration: underline;">
                <img class="card-img-top" src="<%= factura.image_url %>">
              </a>
              <div class="card-body text-center">
                Valor: $<%= factura.valor %>
              </div>
              <div class="card-footer">
                <form action="/delete-entry/<%= factura.id %>" method="POST" onsubmit="return confirm('¿Estás seguro de que deseas eliminar esta factura?');" style="display: inline;">
                  <button type="submit" class="btn btn-danger btn-block">Delete</button>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="card card-body text-center">
          <p>Ninguna Factura disponible <a href="/new">Agregar nueva</a></p>
        </div>
      <% } %>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
